{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "buildCommand": "cd frontend && npm ci && npm run build",

  "functions": {
    "app/app.py": {
      "excludeFiles": "{frontend/node_modules/**,frontend/src/**,frontend/.env*,data/**,chroma-db/**,.venv/**,venv/**,tests/**}"
    }
  }
}
